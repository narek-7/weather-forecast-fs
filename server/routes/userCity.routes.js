const express = require("express");
const router = express.Router({ mergeParams: true });

router.post("/userCity", async (req, res) => {
   
});

router.get("/userCity/:email", async (req, res) => {
   
});

module.exports = router;
